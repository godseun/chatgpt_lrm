query{
    reportData {
        report(code:"${code}") {
            events(fightIDs: [${fids}], dataType: ${dType}, encounterID: ${nid}, sourceID: ${sid}, abilityID: ${aid}) {
                data
            }
        }
    }
}