query{
    characterData {
        character(name:"다순이", serverSlug:"azshara", serverRegion:"KR") {
            recentReports {
                data {
                    code
                    fights(killType: Encounters) {
                        encounterID
                        kill
                        id
                        name
                    }
                }
            }
        }
    }
}