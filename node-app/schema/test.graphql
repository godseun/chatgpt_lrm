query{
    worldData {
        encounter(id: 3009) {
            id
            name
            characterRankings(
                metric: dps, 
                difficulty: 5, 
                className: "Mage", 
                specName: "Fire", 
                serverRegion: "KR",
                page: 5
            )
        }
    }
}